# ============================================================
# GitHub 热榜项目分析器 - 后端依赖
# ============================================================

# ---------- 核心框架 ----------
# FastAPI：现代化、高性能的 Web 框架
fastapi==0.104.1
# Uvicorn：ASGI 服务器，用于运行 FastAPI 应用
uvicorn==0.24.0

# ---------- 数据库 ----------
# SQLAlchemy：Python ORM 框架，用于数据库操作
# 2.0.31+ 正式支持 Python 3.13；2.0.23 在 3.13 下会因
# TypingOnly.__init_subclass__ 断言报错（__static_attributes__/__firstlineno__）
SQLAlchemy==2.0.36
# aiosqlite：SQLite 异步驱动，支持 SQLAlchemy 异步模式
aiosqlite==0.19.0
# 注：SQLite 数据库引擎是 Python 内置模块（sqlite3），无需额外安装

# ---------- 数据验证 ----------
# Pydantic：数据验证和设置管理，使用 v1 版本保持兼容
pydantic==1.10.13

# ---------- HTTP 请求 ----------
# httpx：异步 HTTP 客户端，用于调用 GitHub API 和 AI API
httpx==0.25.2
# requests：同步 HTTP 客户端，备用或简单场景使用
requests==2.31.0

# ---------- AI 大模型 ----------
# OpenAI SDK：兼容 OpenAI 协议的大模型 SDK（通义千问、DeepSeek 等）
openai==1.3.7
# tiktoken：Token 计数器（如需使用可取消注释，Python 3.13 需安装 Rust 编译器）
# tiktoken==0.5.2

# ---------- 工具库 ----------
# python-dotenv：从 .env 文件加载环境变量
python-dotenv==1.0.0
# python-multipart：FastAPI 文件上传支持
python-multipart==0.0.6
# beautifulsoup4：HTML 解析库，用于解析 README 等网页内容
beautifulsoup4==4.12.2
# markdown：Markdown 文本解析
markdown==3.5.1
# PyYAML：YAML 配置文件解析
PyYAML==6.0.1

# ---------- 日志 ----------
# loguru：优雅的 Python 日志库
loguru==0.7.2

# ---------- 测试 ----------
# pytest：自动化测试框架，运行 backend/tests 下的用例
pytest==8.2.2
